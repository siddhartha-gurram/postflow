# =============================================================================
# PostFlow Backend — Example Environment Variables
# =============================================================================
# Copy to .env and fill in values. Never commit .env.
# =============================================================================

# -----------------------------------------------------------------------------
# LinkedIn Provider (OAuth 2.0 + API v2)
# -----------------------------------------------------------------------------
# Create an app at https://www.linkedin.com/developers/apps
# Auth tab: add Redirect URL (must match exactly, e.g. https://api.postflow.app/v1/connections/callback/linkedin)
# Products: ensure "Sign In with LinkedIn using OpenID Connect" and "Share on LinkedIn" (or Marketing API if using analytics) are added.

# Required
LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
LINKEDIN_REDIRECT_URI=https://api.postflow.app/v1/connections/callback/linkedin

# Optional: space-separated scopes (default: openid profile email w_member_social)
# Add r_member_postAnalytics if your app has Marketing Developer Platform access for post analytics.
# LINKEDIN_SCOPES=openid profile email w_member_social r_member_postAnalytics

# =============================================================================
# Other (placeholder for full app)
# =============================================================================
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://...
# REDIS_URL=redis://...
# TOKEN_ENCRYPTION_KEY=base64_encoded_32_byte_key
# STRIPE_SECRET_KEY=sk_...
# FRONTEND_URL=https://app.postflow.com
